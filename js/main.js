var slider = new MasterSlider();
slider.setup('masterslider', {
        width:1086,
        height:720,
        space:0,
        autoplay:true,
        loop:true,
        overPause:false,
        speed:30,
        view:'mask'
    });